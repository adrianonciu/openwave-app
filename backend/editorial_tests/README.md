# Editorial Benchmarks

This folder stores curated, versioned bulletin references for manual editorial QA and regression review.

- `backend/debug_output/` remains the generated local output area.
- Files in this folder are intentional benchmark copies that stay tracked in Git.
